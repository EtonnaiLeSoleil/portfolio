<?php
/** @var string $content */
/** @var string|null $pageTitle */
/** @var string|null $brandTitle */
/** @var array<int, array{href:string,label:string}>|null $navLinks */
?>
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title><?= e($pageTitle ?? 'Portfolio'); ?></title>
    <meta name="description" content="Portfolio de Antoine Gouet - Développeur Web" />

    <link rel="stylesheet" href="css/style.css" />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Source+Sans+Pro:wght@400;600;700;900&display=swap"
      rel="stylesheet"
    />
    <link rel="icon" href="img/favicon.svg" type="image/svg+xml" />
</head>
<body>
    <header class="header">
      <div class="header__content">
        <div class="header__logo-container">
          <div class="header__logo-img-cont">
             <img
              src="img/logo.jpg"
              alt="Logo Antoine Gouet"
              class="header__logo-img"
            />
          </div>
          <span class="header__logo-sub"><?= e($brandTitle ?? 'Antoine Gouet'); ?></span>
        </div>
        <div class="header__main">
          <ul class="header__links">
            <?php if (isset($navLinks) && is_array($navLinks)): ?>
                <?php foreach ($navLinks as $link): ?>
                <li class="header__link-wrapper">
                  <a class='header__link' href='<?= e($link['href']); ?>'> <?= e($link['label']); ?> </a>
                </li>
                <?php endforeach; ?>
            <?php else: ?>
                <li class="header__link-wrapper">
                  <a class='header__link' href='index.php'> Accueil </a>
                </li>
            <?php endif; ?>
          </ul>
          <div class="header__main-ham-menu-cont" id="mobile-menu-btn">
            <!-- Mobile Menu Icon (Simplified) -->
            <div style="font-size: 2rem; cursor: pointer; color: white;">&#9776;</div>
          </div>
        </div>
      </div>

      <!-- Mobile Menu -->
      <div class="header__sm-menu" id="mobile-menu">
        <ul class="header__sm-menu-links" style="list-style: none; padding: 0; margin: 0;">
          <?php if (isset($navLinks) && is_array($navLinks)): ?>
              <?php foreach ($navLinks as $link): ?>
              <li class="header__sm-menu-link">
                <a href="<?= e($link['href']); ?>"><?= e($link['label']); ?></a>
              </li>
              <?php endforeach; ?>
          <?php else: ?>
              <li class="header__sm-menu-link">
                <a href="index.php">Accueil</a>
              </li>
          <?php endif; ?>
        </ul>
      </div>
    </header>

    <?= $content; ?>

    <footer class="main-footer">
      <div class="main-container">
        <div class="main-footer__upper">
          <div class="main-footer__row main-footer__row-1">
            <h2 class="heading heading-sm main-footer__heading-sm">
              <span>Réseaux</span>
            </h2>
            <div class="main-footer__social-cont">
              <a target="_blank" rel="noreferrer" href="#">
                <img
                  class="main-footer__icon"
                  src="img/favicon.svg"
                  alt="LinkedIn"
                  style="filter: invert(1); width: 24px;"
                />
              </a>
              <a target="_blank" rel="noreferrer" href="#">
                <img
                  class="main-footer__icon"
                  src="img/favicon.svg"
                  alt="GitHub"
                  style="filter: invert(1); width: 24px;"
                />
              </a>
            </div>
          </div>
          <div class="main-footer__row main-footer__row-2">
            <h4 class="heading heading-sm text-lt"><span style="white-space: nowrap;"><?= e($brandTitle ?? 'Antoine Gouet'); ?></span></h4>
            <p class="main-footer__short-desc">
              <span>Développeur Web Full Stack passionné par la création d'applications utiles et performantes.</span>
            </p>
          </div>
        </div>
        <div class="main-footer__lower">
          &copy; <?= date('Y'); ?>. Made with PHP & &hearts;
        </div>
      </div>
    </footer>

    <script>
      const mobileMenuBtn = document.getElementById('mobile-menu-btn');
      const mobileMenu = document.getElementById('mobile-menu');

      if (mobileMenuBtn && mobileMenu) {
        mobileMenuBtn.addEventListener('click', () => {
          mobileMenu.classList.toggle('header__sm-menu--active');
        });
        
        // Fermer le menu au clic sur un lien
        const mobileLinks = mobileMenu.querySelectorAll('a');
        mobileLinks.forEach(link => {
          link.addEventListener('click', () => {
            mobileMenu.classList.remove('header__sm-menu--active');
          });
        });
      }
    </script>
</body>
</html>
